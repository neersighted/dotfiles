# PulseAudio
{_,ctrl,shift,shift + ctrl} + XF86AudioRaiseVolume
  ponymix -N increase {1,2,5,10}
{_,ctrl,shift,shift + ctrl} + XF86AudioLowerVolume
  ponymix -N decrease {1,2,5,10}
XF86AudioMute
  ponymix toggle

# Flameshot
{_,shift} + Print
  flameshot {gui,full -c}

# MPD Playback
XF86Audio{Play,Stop,Next,Prev}
  mpc {toggle,stop,next,prev}
