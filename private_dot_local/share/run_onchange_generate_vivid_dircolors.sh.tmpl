{{- if lookPath "vivid" -}}
#!/bin/sh -e

echo 'Regenerating cached vivid colors...'
vivid generate nord > "${XDG_DATA_HOME:-$HOME/.local/share}/vivid.dircolors"

# run_onchange hash: {{ output "vivid" "--version" | sha256sum }}
{{- end -}}
