LICENSE
README.md
bootstrap.sh
tags

.local/bin/*.c

{{ if ne .chezmoi.os "windows" }} # ignore windows files on *nix
AppData
bin
{{ else }} # ignore (most) *nix files on windows
.config/*
!.config/git
.local
.gnupg
{{ end }}

{{ if eq .chezmoi.os "darwin" }} # ignore x11 files on macos
.config/xinit/**
.config/Xresources
{{ else }} # terminfo hacks are only needed on macos
.terminfo
{{ end }}

{{ if or (ne .chezmoi.os "linux") (eq (index .chezmoi.osRelease "id") "arch") }} # hushlogin on everything but arch
.hushlogin
{{ end }}
