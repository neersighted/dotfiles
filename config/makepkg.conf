#
# ~/.makepkg.conf
#

#
# Downloading
#

# Set the downloading programs.
DLAGENTS=('ftp::/usr/bin/curl -fC - --ftp-pasv --retry 3 --retry-delay 3 -o %o %u'
          'http::/usr/bin/curl -fLC - --retry 3 --retry-delay 3 -o %o %u'
          'https::/usr/bin/curl -fLC - --retry 3 --retry-delay 3 -o %o %u'
          'rsync::/usr/bin/rsync -z %u %o'
          'scp::/usr/bin/scp -C %u %o')

#
# Compiling
#

export CC="clang"
export CXX="clang++"

# Build for the right arch.
case "$(uname -m)" in
  (i686)
    CARCH="i686"
    CHOST="i686-pc-linux-gnu"
  ;;
  (x86_64)
    CARCH="x86_64"
    CHOST="x86_64-unknown-linux-gnu"
  ;;
esac

MAKEFLAGS="-j`nproc`"
CFLAGS="-march=native -O2 -pipe -fstack-protector --param=ssp-buffer-size=4"
CXXFLAGS="-march=native -O2 -pipe -fstack-protector --param=ssp-buffer-size=4"
DEBUG_CFLAGS="-g -fvar-tracking-assignments"
DEBUG_CXXFLAGS="-g -fvar-tracking-assignments"
CPPFLAGS="-D_FORTIFY_SOURCE=2"
LDFLAGS="-Wl,-O1,--sort-common,--as-needed,-z,relro"

#
# Building/Packaging
#

# Build/package with fancy options.
BUILDENV=(fakeroot !distcc color ccache check sign)
OPTIONS=(strip docs libtool staticlibs emptydirs zipman purge !upx !debug)
INTEGRITY_CHECK=(md5 sha1 sha256)

# Strip as many symbols as possible.
STRIP_BINARIES="--strip-all"
STRIP_SHARED="--strip-unneeded"
STRIP_STATIC="--strip-debug"

# Process documentation files.
DOC_DIRS=(usr/{,local/}{,share/}{doc,gtk-doc} opt/*/{doc,gtk-doc})
MAN_DIRS=({usr{,/local}{,/share},opt/*}/{man,info})
# Purge extra files.
PURGE_TARGETS=(usr/{,share}/info/dir .packlist *.pod)

#
# Output
#

# Output to the right directories.
#BUILDDIR="$HOME/Packages/Build"
#SRCDEST="$HOME/Packages/Sources"
#PKGDEST="$HOME/Packages/Builds"
#SRCPKGDEST="$HOME/Packages/Source Builds"
#LOGDEST="$HOME/Packages/Logs"

# Compression options.
COMPRESSGZ=(gzip -c -f -n)
COMPRESSBZ2=(bzip2 -c -f)
COMPRESSXZ=(xz -c -z -)
COMPRESSLRZ=(lrzip -q)
COMPRESSLZO=(lzop -q)
COMPRESSZ=(compress -c -f)

# Compress with xz.
PKGEXT=".pkg.tar.xz"
SRCEXT=".src.tar.gz"
