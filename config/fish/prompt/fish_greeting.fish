function fish_greeting
  # if status is-login; and macos?; and not tmux?
  #   if brew upgrade --dry-run | read -z text
  #     printf 'New packages are available: %s\n' $text
  #     printf 'Run `brew upgrade` now?'
  #     read -P '(Y/n)' upgrade
  #     test "$upgrade" = 'y'; and brew upgrade
  #   end
  # end
end
