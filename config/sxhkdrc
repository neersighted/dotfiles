# ALSA Audio
{_,shift,shift + ctrl} + XF86Audio{Raise,Lower}Volume
  amixer set $ALSA_MIXER {1+,5+,10+,1-,5-,10-}
XF86AudioMute
  amixer set $ALSA_MIXER toggle

# Flameshot
{_,shift} + Print
  flameshot {gui,full -c}

# MPD Playback
XF86Audio{Play,Stop,Next,Prev}
  mpc {toggle,stop,next,prev}
