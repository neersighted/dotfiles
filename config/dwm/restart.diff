diff --git a/dwm.c b/dwm.c
index f896170..25fe1b8 100644
--- a/dwm.c
+++ b/dwm.c
@@ -259,6 +259,7 @@ static void (*handler[LASTEvent]) (XEvent *) = {
 };
 static Atom wmatom[WMLast], netatom[NetLast];
 static Bool running = True;
+static Bool restart = False;
 static Cur *cursor[CurLast];
 static ClrScheme scheme[SchemeLast];
 static Display *dpy;
@@ -1231,6 +1232,8 @@ propertynotify(XEvent *e) {
 
 void
 quit(const Arg *arg) {
+	if(arg->i)
+		restart = True;
 	running = False;
 }
 
@@ -2066,6 +2069,8 @@ main(int argc, char *argv[]) {
 	setup();
 	scan();
 	run();
+	if(restart)
+		execvp(argv[0], argv);
 	cleanup();
 	XCloseDisplay(dpy);
 	return EXIT_SUCCESS;
