[Unit]
Description = X Keyboard Mapping Modifer
Requires    = xorg.target
After       = xorg.target

[Service]
Type        = oneshot
ExecStart   = /usr/bin/xmodmap %h/.Xmodmap

[Install]
WantedBy = user-programs.target
