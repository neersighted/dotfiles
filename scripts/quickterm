#!/usr/bin/env bash

CLASS="$0"

# Check if the terminal exists.
if id=$(xdo id -N "$CLASS"); then
  if [[ $id == $(xdo id) ]]; then
    # If it is the current window, hide it.
    xdo hide "$id"
  else
    # Otherwise make it visible/active.
    xdo show "$id"
    xdo activate "$id"
  fi
else
  # It's not running, so start a new one.
  exec "$TERMINAL" -t "$CLASS" -e "$SHELL"
fi

