XDG_STATE_HOME = ENV.has_key?("XDG_STATE_HOME") ? ENV["XDG_STATE_HOME"] : File.join(Dir.home, ".local", "state")

IRB.conf[:SAVE_HISTORY] ||= 1000
IRB.conf[:HISTORY_FILE] ||= File.join(XDG_STATE_HOME, "irb", "history")
