#!/usr/bin/env fish

set majors (string replace -rf '.*/(\d+\.\d+).*' '$1' $PYENV_ROOT/versions/* | uniq)

for major in $majors
  set_color yellow
  echo "Pruning Python $major..."
  set_color normal

  pyenv latest uninstall $major
end
