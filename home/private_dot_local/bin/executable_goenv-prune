#!/usr/bin/env fish

set majors (string replace -rf '.*/(\d+\.\d+).*' '$1' $GOENV_ROOT/versions/* | uniq)

for major in $majors
  set_color blue
  echo "Pruning Go $major..."
  set_color normal

  goenv latest uninstall $major
end
