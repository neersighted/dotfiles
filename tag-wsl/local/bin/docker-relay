#!/bin/sh

NPIPERELAY=$(which npiperelay.exe)

if [ ! -f /var/run/docker.sock ]; then
  sudo socat UNIX-LISTEN:/var/run/docker.sock,fork,group=docker,umask=007 EXEC:"$NPIPERELAY -ep -s //./pipe/docker_engine",nofork
fi
