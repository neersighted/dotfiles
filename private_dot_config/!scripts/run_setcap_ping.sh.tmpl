{{- if .wsl -}}
{{- $ping := lookPath "ping" -}}
{{- if (not (output "getcap" $ping)) -}}
#!/bin/sh -e

echo "Adding cap_net_raw to ping..."
sudo setcap cap_net_raw+p "$(realpath "{{ $ping }}")"

# vi: ft=sh
{{- end -}}
{{- end -}}
