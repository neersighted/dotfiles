{{- if lookPath "goenv" -}}
function goenv -d 'go environment manager'
  set -lx GOENV_SHELL fish
  set command $argv[1]

  switch $command
  case rehash shell
    set -e argv[1]
    command goenv sh-$command $argv | source
  case '*'
    command goenv $argv
  end
end
{{- end -}}
