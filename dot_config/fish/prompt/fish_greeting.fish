function fish_greeting
  # if status is-login; and is_macos; and not is_tmux
  #   if brew upgrade --dry-run | read -z text
  #     printf 'New packages are available!\n'
  #     printf '%s' $text
  #     read -P '(Y/n) ' upgrade
  #     test "$upgrade" != 'n'; and brew upgrade
  #   end
  # end
end
